@using HotelManagement.DataTransferObjectLayer.DTOs.RoomDTOs
@model List<ResultRoomDTO>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/_AdminLayout.cshtml";
}

<div class="col-lg-12 d-flex align-items-stretch">
    <div class="card w-100">
        <div class="card-body p-4">
            <div class="d-flex mb-4 justify-content-between align-items-center">
                <h5 class="mb-0 fw-bold">Oda Listesi</h5>
            </div>
           
            <div class="table-responsive" data-simplebar>
                <table class="table table-borderless align-middle text-nowrap">
                    <thead>
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">Görsel</th>
                            <th scope="col">Oda No</th>
                            <th scope="col">Başlık</th>
                            <th scope="col">Fiyat</th>
                            <th scope="col">İşlemler</th>
                        </tr>
                    </thead>
                    <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                <td><span class="fs-3 fw-normal">@item.Id</span></td>
                                    <td>
                                        <div class="d-flex align-items-center">
                                        @if (!string.IsNullOrEmpty(item.CoverImage))
                                            {
                                            <img src="@item.CoverImage" width="50" height="50" class="rounded object-fit-cover" alt="@item.Title" />
                                            }
                                            else
                                            {
                                                <div class="rounded bg-light d-flex align-items-center justify-content-center" style="width:50px;height:50px;">
                                                    <i class="ti ti-photo fs-4 text-muted"></i>
                                                </div>
                                            }
                                        </div>
                                    </td>
                                <td><p class="fs-3 fw-normal mb-0">@item.RoomNumber</p></td>
                                <td><p class="fs-3 fw-normal mb-0">@item.Title</p></td>
                                <td><p class="fs-3 fw-normal mb-0">@item.Price ₺</p></td>
                                    <td>
                                    <a href="/Admin/Room/DeleteRoom/@item.Id" class="btn btn-danger">Sil</a>
                                    <a href="/Admin/Room/UpdateRoom/@item.Id" class="btn btn-warning">Düzenle</a>
                                        <a href="#" class="btn btn-primary">Detaylar</a>
                                    </td>
                                </tr>
                            }
                    </tbody>
                </table>
            </div>
            <a href="/Admin/Room/InsertRoom" class="btn btn-success">Yeni Oda</a>
        </div>
    </div>
</div>